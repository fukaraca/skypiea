apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skypiea-ai.fullname" . }}-test-connection"
  labels:
    {{- include "skypiea-ai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skypiea-ai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
